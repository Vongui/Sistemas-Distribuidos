syntax = "proto3";

package validador;

service VerificarDocumento {
  rpc Verifica (Requisicao) returns (Resposta);
}

message Requisicao {
  string tipo = 1;
  string numero = 2;
}

message Resposta {
  bool valido = 1;
  string mensagem = 2;
}